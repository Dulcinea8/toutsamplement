<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet"  href="{{ asset('css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    <link rel="icon" href="{{ asset('images/logo4.png') }}">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.1/css/all.css" integrity="sha384-O8whS3fhG2OnA5Kas0Y9l3cfpmYjapjI0E4theH4iuMD+pLhbf6JI0jIMfYcK3yZ" crossorigin="anonymous">


    <title>{% block title %}{% endblock %}</title>
</head>

<body class="container-fluid">

    <header class="container-fluid p-5">

        <nav class="text-center">
            <a class="navbar-brand" href="{{ path('accueil')}}"> | Accueil |</a>
            <a class="navbar-brand" href="{{ path('all-articles')}}"> | Articles |</a>
            <a class="navbar-brand" href=""> | Samples |</a>
            <a class="navbar-brand" href=""> | Inserer |</a>
            <a class="navbar-brand" href="{{ path('contact')}}"> | Contact |</a>


            {# l'element du menu n'apparaitra que si l'utilisateur est connecté #}


                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="navbar-brand" href="{{ path('login') }}"> |  Connexion / Inscription | </a>
                {% endif %}

            {# l'element du menu n'apparaitra que si l'utilisateur est connecté #}

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="navbar-brand" href="{{ path('logout') }}"> |  Deconnexion | </a>
                    <a class="navbar-brand" href=""> |  Profil | </a>
                {% endif %}

            {# l'element du menu n'apparaitra que si l'utilisateur est connecté et a le role admin #}

                {% if is_granted('ROLE_ADMIN') %}
                     <a class="navbar-brand" href="{{ path('admin') }}"> |  Administrateur | </a>
                {% endif %}

            <input class="mr-1" placeholder="Titre / Artiste"></input>
        </nav>


        

    </header>
    <h1 class="text-center">{% block titlepage %}{% endblock %}</h1>
    <div class="font container justify-content-center">

        {% block content %}{% endblock %}

    </div>

    <footer class="container-fluid">

        <p>Conditions d'utilisation</p>
        <i class="fab fa-twitter-square"></i>
        <i class="fab fa-facebook-square"></i>
        <i class="fab fa-instagram"></i>

    </footer>

    <script src="{{ asset('js/jquery-3.3.1.js') }}"></script>

    {% block js %}{% endblock %}



</body>
</html>

