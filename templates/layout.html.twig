<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet"  href="{{ asset('css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    <link rel="icon" href="{{ asset('images/logo4.png') }}">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.1/css/all.css" integrity="sha384-O8whS3fhG2OnA5Kas0Y9l3cfpmYjapjI0E4theH4iuMD+pLhbf6JI0jIMfYcK3yZ" crossorigin="anonymous">
    <link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <link href="https://fonts.googleapis.com/css?family=Permanent+Marker%7CRock+Salt" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Monoton" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Bangers%7CMonoton" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Allerta+Stencil%7CBangers%7CCabin+Sketch%7CCovered+By+Your+Grace%7CMonoton" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Allerta+Stencil%7CBangers%7CCabin+Sketch%7CCovered+By+Your+Grace%7CFaster+One%7CMonoton" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Allerta+Stencil%7CBangers%7CCabin+Sketch%7CCovered+By+Your+Grace%7CExpletus+Sans%7CFaster+One%7CMonoton" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Timmana" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Zilla+Slab+Highlight" rel="stylesheet">

    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">




    <title>{% block title %}{% endblock %}</title>
</head>

<body>

    <header>
        <nav class="row no-gutters align-bottom fixed-top bgnav p-3">
            <div class="col-0 col-md-2 d-flex inline">
                <a href="{{ path('accueil')}}">
                    <img src="../images/LogoSeul.png" width="70">
                    <h6>Tout samplement</h6>
                    <!-- <img src="../images/LogoTitre(1).png" width="160"> -->
                </a>
            </div>
            <div class="col-12 col-md-6 no-gutters mainnav mt-2">
            <ul class="d-flex no-gutters justify-content-between">
                <li class="">
                    <a class="" href="{{ path('accueil')}}"> Accueil</a>
                </li>
                <li class="">
                        <a class="" href="{{ path('all-articles')}}"> Articles</a>
                </li>
                <li class="">
                        <a class="" href="{{ path('all-samples')}}">Samples</a>
                </li>
                <li class="">
                        <a class="" href="{{ path('inserer')}}">Insérer</a>
                </li>
                 {% if not is_granted('ROLE_ADMIN') %}
                    <li class="">
                        <a class="" href="{{ path('contact')}}"> Contact </a>
                    </li>
                    {% endif %}

                    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="">
                            <a class="" href="{{ path('login') }}"> Connexion/Inscription </a>
                        </li>
                    {% endif %}

                    {# l'element du menu n'apparaitra que si l'utilisateur est connecté en user#}

                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="">
                            <a class="" href="{{ path('profil') }}">Profil </a>
                        </li>
                        <li class="">
                            <a class="" href="{{ path('logout') }}"> Deconnexion </a>
                        </li>
                    {% endif %}

                    {# l'element du menu n'apparaitra que si l'utilisateur est connecté en admin#}

                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="">
                            <a class="" href=""> Administrateur </a>
                        </li>
                    {% endif %}
                    </div>

                    <div class="offset-2"></div>
                    <div class="col-12  col-md-2 text-right">
        {#FORMULAIRE DE RECHERCHE#}
        <form class="form-inline test mr-4 mt-2" id="cherche">
            <div class="input-group">
                <div class="input-group-prepend">
                    <span class="input-group-text" id="basic-addon1">Search</span>
                </div>
                <input type="text" id="recherche" name="recherche" class="form-control" placeholder="Track or Artist" aria-label="Username" aria-describedby="basic-addon1">
                <div id="result"></div>
            </div>

        </form>
    </div>
            </ul>
        </nav>
    </header>


<h1 class="text-center">{% block titlepage %}{% endblock %}</h1>
<div class="font container justify-content-center rounded mt-4">


    {% block content %}{% endblock %}

</div>
<br>
<br>
<br>
<br>
<br>
<br>


<footer class="container-fluid">

    <div class="mt-5 d-flex justify-content-between">

        <a class="text-danger" href="{{ path('cgu')}}">Conditions d'utilisation</a>
        <div class="">
            <i class="mr-2 text-danger fab fa-twitter-square fa-lg"></i>
            <i class="mr-2 text-danger fab fa-facebook-square fa-lg"></i>
            <i class="mr-2 text-danger fab fa-instagram fa-lg"></i>
        </div>

    </div>

</footer>

<script src="{{ asset('js/jquery-3.3.1.js') }}"></script>
    <script src="{{ asset('js/bootstrap.min.js') }}"></script>
<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
{% block js %}{% endblock %}

    {#//ici je fait la requete Ajax pour le moteur de recherche de mon menu#}
    <script>
        $(function () {

            $('#recherche').on('keyup', function () {

                //if ($(this).val().length > 1)
                //{

                $('#recherche').on( "mouseleave", function(){
                    ObjMenuTimeout  = setTimeout( function(){
                        $('#result').hide();
                    }, 1500 );
                });

                $('#recherche').on( "mouseenter", function(){
                    clearTimeout( ObjMenuTimeout );
                });

                 $.ajax({
                        type: 'get',
                        url: "{{path('ajax-search')}}",
                        data: "recherche=" + $(this).val(),
                        timeout: 3000,
                        dataType: 'html',
                        success: function (data) {
                            //$('#result').empty();
                            $('#result').html(data);

                            console.log(data);
                        },
                        error: function (error) {
                            console.log(error.responseText);
                        }
                    })


               // }
            })
        })
    </script>





</body>
</html>
